using VContainer;
using VContainer.Unity;

namespace unity.vcontainer.example
{
    public class RootScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}